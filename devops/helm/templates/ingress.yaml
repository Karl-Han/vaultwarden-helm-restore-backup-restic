{{- if .Values.vaultwarden.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "helm.fullname" . }}
  labels:
    {{- include "helm.labels" . | nindent 4 }}
    app.kubernetes.io/component: vaultwarden
  {{- with .Values.vaultwarden.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.vaultwarden.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.vaultwarden.ingress.tls }}
  tls:
    {{- range .Values.vaultwarden.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.vaultwarden.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "helm.fullname" $ }}
                port:
                  number: {{ $.Values.vaultwarden.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
